# WF-12 Logging
Write `INFO/WARN/ERROR` to `logs/app.log`.
Record request time, route/date, page, HTTP status, retry count, result count, elapsed time, final state.
Never record API key or sensitive environment values.
